kind: api
apiVersion: api.webmethods.io/v1
metadata:
  name: PaymentAPI
  namespace: dev
  version: 1.0
  tags:
    - swagger
    - daso
    - sample
  type: REST
spec:
  mocked: true
  api-spec:
    $path: petstore.yaml
  policy-sequence:
    - $ref: dev:dev_policiess:1.0